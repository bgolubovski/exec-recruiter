# Geo rules and CV header conditional
# Defines which geographies are in-scope for sourcing and how the CV header reads per geo.

# Default user location - stays the same regardless of role geo
home_base: "Lisbon, Portugal"

# Citizenship / work-authorization signals to surface in the CV header
citizenship: "US Citizen"

# In-scope regions for sourcing (LinkedIn search location filter)
sourcing_regions:
  - id: us-remote
    label: "United States"
    linkedin_filter: "f_WT=2,3"   # Remote + Hybrid
    accept_any_modality: true
  - id: uk
    label: "United Kingdom"
    linkedin_filter: ""           # all modalities; we'll filter at score time
    accept_any_modality: true
  - id: eu
    label: "European Union"
    sub_regions:
      - Germany
      - Netherlands
      - Spain
      - France
      - Ireland
      - Portugal
    accept_any_modality: true
  - id: dubai
    label: "United Arab Emirates"
    accept_any_modality: false
    require_explicit_approval: true
  - id: israel
    label: "Israel"
    accept_any_modality: false
    require_explicit_approval: true

# CV header conditional logic
# The plugin chooses the header line based on the role's geo.
cv_header_templates:
  us: "Lisbon, Portugal (US Citizen)"
  uk: "Lisbon, Portugal (US Citizen, Open to UK Relocation)"
  eu_non_uk: "Lisbon, Portugal"
  dubai: "Lisbon, Portugal"            # do not claim Dubai relocation without explicit approval
  israel: "Lisbon, Portugal"           # do not claim Tel Aviv relocation without explicit approval

# Geo friction flags - surfaced during scoring
on_site_only_outside_home: skip      # always skip on-site-only outside the home metro
hybrid_3_plus_days: friction          # flag, do not auto-skip
relocation_required_outside_us_uk: require_explicit_approval

# Strict rules - must never be silently violated
strict_rules:
  - "Never claim Tel Aviv relocation without explicit user approval"
  - "Never claim Dubai relocation without explicit user approval"
  - "Default to home_base for any unspecified geo"
